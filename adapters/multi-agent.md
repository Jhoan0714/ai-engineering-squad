# Multi-agent runtime adapters

**Status:** placeholder — content in backlog **E3**.

Will document how to map each role to an isolated agent workspace (e.g. OpenClaw: `SOUL.md`, tool allowlists, bindings).
