# Skills

Shared skills loaded by one or more roles.

Each skill lives in `skills/<skill-id>/SKILL.md` (portable skill format with YAML frontmatter).

Core MVP skills are backlog **C1–C4** (separate PRs). See [docs/layout.md](../docs/layout.md).
